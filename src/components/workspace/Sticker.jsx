import React from "react";
import { motion } from "framer-motion";
import { ACCENTS } from "@/lib/workspace-data";

export default function Sticker({ label, accent = "yellow", rot = -4, className = "" }) {
  return null;









}